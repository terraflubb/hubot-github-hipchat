<a href="{{= it.repo.url }}">{{= it.repo.name }}</a>:
<a href="{{= it.who.url }}">{{= it.who.name }}</a>
created issue <a href="{{= it.issue.url }}">#{{= it.issue.number }}</a>:
{{= it.issue.title }}
