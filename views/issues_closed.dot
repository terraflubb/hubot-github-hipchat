<a href="{{= it.repo.url }}">{{= it.repo.name }}</a>:
<a href="{{= it.who.url }}">{{= it.who.name }}</a>
has closed issue
<a href="{{= it.issue.url }}">#{{= it.issue.number }}</a>:
"{{= it.issue.title }}"